# API Reference — {{name}}

> {{description}}

**Version:** `{{version}}`
**Language:** {{language}}{{#unless (eq framework "None")}} · **Framework:** {{framework}}{{/unless}}

---

{{#if (hasItems exports)}}
{{#each exports}}
## `{{name}}`

**Kind:** `{{kind}}`

{{#if signature}}
```typescript
{{signature}}
```
{{/if}}

> No description available. Add a JSDoc comment above this export.

---

{{/each}}
{{else}}
No exported symbols were detected automatically.

To get richer API docs:
- Add JSDoc `/** ... */` comments to your exports
- Use explicit `export` keywords on all public API surfaces
- Re-run `ai-readme-gen . --type api`
{{/if}}
